name: Mobile Smoke Tests

on:
  push:
    branches:
      - tests-only-branch   # запускать CI только при пуше в tests-only-branch
  pull_request:
    branches:
      - tests-only-branch   # можно добавить и для PR в эту ветку

jobs:
  mobileSmokeTests:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: Install dependencies
        run: npm ci

      - name: Run mobileSmoke tests
        run: npx playwright test --grep "@mobile1" --reporter=list --headed

      - name: Upload Playwright Test Report
        uses: actions/upload-artifact@v3
        with:
          name: playwright-report
          path: playwright-report/
